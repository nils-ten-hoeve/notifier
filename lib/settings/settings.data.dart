import 'package:notifier/settings/settings.domain.dart';

class SettingsDatabase {
  Settings read() {
    throw Exception('To be implemented (as synch function, using YAML)');
  }

  write(Settings settings) {
    throw Exception('To be implemented (as synch function, using YAML)');
  }
}
